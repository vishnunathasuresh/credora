{
  "name": "@credora/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env.local ../../node_modules/tsx/dist/cli.mjs watch src/index.ts",
    "build": "tsc -p tsconfig.json --outDir dist",
    "start": "node --env-file-if-exists=../../.env.local dist/index.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@credora/auth": "workspace:*",
    "@credora/blockchain": "workspace:*",
    "@credora/credential-core": "workspace:*",
    "@credora/demo-data": "workspace:*",
    "@credora/shared": "workspace:*",
    "@credora/storage": "workspace:*",
    "viem": "^2.31.3",
    "tsx": "^4.20.3"
  }
}
